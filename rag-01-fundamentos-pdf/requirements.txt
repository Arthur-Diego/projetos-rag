# Versões conferidas contra o PyPI em 25/07/2026 — todas batem com ../README.md
# Instale DENTRO do venv: não há pip no sistema.
#   python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

langchain==1.3.14
langchain-openai==1.4.1
langchain-text-splitters==1.1.2
langchain-chroma==1.1.0
langchain-community==0.4.2
pypdf==6.14.2
python-dotenv==1.2.2

# Verificacao de tipos: obrigatoria, porque os Protocol nao sao
# verificados em runtime. Ver docs/guidelines/arquitetura-em-camadas.md secao 6.
mypy==2.3.0

# API HTTP do contrato compartilhado (docs/contracts/rag-api.yaml).
# Versoes conferidas no PyPI em 25/07/2026.
fastapi==0.140.0
uvicorn==0.51.0
