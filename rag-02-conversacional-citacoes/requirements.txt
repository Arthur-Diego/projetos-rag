# Versões conferidas contra o PyPI em 27/07/2026.
# Instale DENTRO do venv: não há pip no sistema.
#   python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

langchain==1.3.14
langchain-openai==1.4.1
langchain-text-splitters==1.1.2
langchain-qdrant==1.1.0
langchain-community==0.4.2
pypdf==6.14.2
python-dotenv==1.2.2

# Verificacao de tipos: obrigatoria, porque os Protocol nao sao verificados em
# runtime. Ver ../docs/guidelines/arquitetura-em-camadas.md secao 6.
mypy==2.3.0

# API HTTP do contrato compartilhado (../docs/contracts/rag-api.yaml), versao 1.1.0.
fastapi==0.140.1
uvicorn==0.51.0

# Testes: matriz de recusa, reescrita, citacao e camada HTTP, com dubles.
# Nenhum teste toca a API paga. Ver docs/guidelines/README.md.
pytest==9.1.1

# Dependencia REAL da suite: fastapi.testclient e construido sobre httpx.
# Vinha transitivamente pelo SDK da OpenAI, o que e frageil: uma versao futura
# do SDK que largue o httpx quebraria os testes por um motivo sem relacao.
httpx==0.28.1

# Exercicio 3 / criterio de aceite 7 do PRD: trocar o armazem vetorial numa linha.
# Fica aqui, e nao num requirements-dev separado, porque a troca e criterio de
# aceite deste projeto, nao ferramenta de desenvolvimento.
langchain-chroma==1.1.0
