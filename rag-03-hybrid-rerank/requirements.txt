# Versoes conferidas contra o PyPI em 28/07/2026.
# Instale DENTRO do venv: nao ha pip no sistema.
#   python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

langchain==1.3.14
langchain-openai==1.4.1
langchain-text-splitters==1.1.2
langchain-community==0.4.2
pypdf==6.14.2
python-dotenv==1.2.2

# Armazem UNICO do projeto (ADR-001): o Elasticsearch atende os DOIS caminhos de
# busca, kNN denso e BM25, sobre o mesmo indice e o mesmo documento. Substitui o
# langchain-qdrant do Projeto 2.
langchain-elasticsearch==1.0.0

# Reordenacao por cross-encoder, LOCAL e em CPU (ADR-004). Baixa cerca de 500 MB
# de modelo mais torch na primeira execucao. Nao gasta API, e nenhum trecho do
# corpus sai da maquina neste estagio.
#
# O modelo padrao e MULTILINGUE (mmarco), nao o ms-marco ingles do guia da
# trilha: medido sobre este corpus, o ingles derrubava o acerto de 8/10 para
# 5/10. Ver rag/config.py e docs/operations/README.md.
sentence-transformers==5.6.1

# NAO instale rank-bm25, apesar de o guia da trilha o listar. Seria um SEGUNDO
# mecanismo de BM25, in-process e sem persistencia, competindo com o do
# Elasticsearch. Um motor de busca por projeto (ADR-001).

# Verificacao de tipos: obrigatoria, porque os Protocol nao sao verificados em
# runtime. Ver ../docs/guidelines/arquitetura-em-camadas.md secao 6.
mypy==2.3.0

# API HTTP do contrato compartilhado (../docs/contracts/rag-api.yaml), versao 1.2.0.
fastapi==0.140.9
uvicorn==0.51.0

# Testes: fusao RRF, funil de rerank, deduplicacao, matriz de recusa, reescrita,
# citacao e camada HTTP, com dubles. Nenhum teste toca a API paga nem o
# Elasticsearch real. Ver docs/guidelines/README.md.
pytest==9.1.1

# Dependencia REAL da suite: fastapi.testclient e construido sobre httpx.
# Vinha transitivamente pelo SDK da OpenAI, o que e fragil: uma versao futura
# do SDK que largue o httpx quebraria os testes por um motivo sem relacao.
httpx==0.28.1

# O langchain-chroma do Projeto 2 NAO entra: o criterio de aceite de trocar o
# armazem em uma linha saiu de escopo (ADR-004 da feature). Aqui existe um motor
# unico atendendo dois caminhos, e trocar por um que so faz busca densa mataria
# a metade lexica do funil.
